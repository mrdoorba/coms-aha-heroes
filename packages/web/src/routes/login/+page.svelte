<script lang="ts">
  // The +page.server.ts load function always redirects, so this view is
  // only reached if the redirect somehow fails. Keep it minimal.
</script>

<div class="flex min-h-screen items-center justify-center bg-[#0a0e2a] text-white">
  <p class="text-sm opacity-70">Redirecting to the COMS portal…</p>
</div>
